import { HikeDifficultyPipe } from './hike-difficulty.pipe';

describe('HikeDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new HikeDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
